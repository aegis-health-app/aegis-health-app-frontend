import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const MemoryPracticeScreen = () => {
  return (
    <View>
      <Text>MemoryPracticeScreen</Text>
    </View>
  );
};

export default MemoryPracticeScreen;

const styles = StyleSheet.create({});
